{
  "name": "wedding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "pages:dev": "npm run build && npx wrangler pages dev dist",
    "import:invites": "node scripts/import-invites.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
